# --- Main Prompt Format ---
# Scheme: OS(Blue) > Dir(Purple) > Git(Pink) > Langs(Yellow) > Docker(Teal) > System(Grey) > Time(Dark)
format = """
[](fg:#7aa2f7)\
$os\
$username\
[](fg:#7aa2f7 bg:#bb9af7)\
$directory\
[](fg:#bb9af7 bg:#f7768e)\
$git_branch\
$git_status\
[](fg:#f7768e bg:#e0af68)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
$scala\
$php\
$python\
[](fg:#e0af68 bg:#7dcfff)\
$docker_context\
[](fg:#7dcfff bg:#89ddff)\
$kubernetes\
$terraform\
[](fg:#7dcfff bg:#89ddff)\
$aws\
$gcloud\
$azure\
[](fg:#89ddff bg:#414868)\
$jobs\
$cmd_duration\
$status\
$battery\
[](fg:#414868 bg:#24283b)\
$time\
[](fg:#24283b)\
\n$character
"""

# --- User & OS (Blue #7aa2f7) ---
[username]
show_always = false
style_user = "bg:#7aa2f7 fg:#15161e bold"
style_root = "bg:#7aa2f7 fg:#15161e bold"
format = '[$user ]($style)'
disabled = true

[os]
style = "bg:#7aa2f7 fg:#15161e bold"
format = '[$symbol]($style)'
disabled = false
[os.symbols]
Macos = " "
Ubuntu = " "
Windows = " "
Linux = " "

# --- Directory (Purple #bb9af7) ---
[directory]
style = "bg:#bb9af7 fg:#15161e bold"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "

# --- Git (Pink #f7768e) ---
[git_branch]
symbol = ""
style = "bg:#f7768e fg:#15161e bold"
format = '[ $symbol $branch ]($style)'

[git_commit]
style = "bg:#f7768e fg:#15161e bold"
format = '[ \($hash$tag\) ]($style)'
only_detached = true
tag_disabled = false
tag_symbol = " 🏷 "

[git_status]
style = "bg:#f7768e fg:#15161e bold"
format = '[$all_status$ahead_behind ]($style)'
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"
ignore_submodules = true  # Speeds up large repos

# --- Languages (Yellow #e0af68) ---
[c]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[elixir]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[elm]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[golang]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'
detect_extensions = ['go']
detect_files = ['go.mod', 'go.sum']

[gradle]
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[java]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[julia]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'
detect_extensions = ['js', 'mjs', 'cjs', 'ts']
detect_files = ['package.json', '.node-version', '.nvmrc']

[nim]
symbol = "󰆥 "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[rust]
symbol = ""
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'
detect_extensions = ['rs']
detect_files = ['Cargo.toml', 'Cargo.lock']

[scala]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[php]
symbol = ""
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version) ]($style)'

[python]
symbol = " "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol($version )(\($virtualenv\) )]($style)'
detect_extensions = ['py']
detect_files = ['.python-version', 'Pipfile', 'requirements.txt', 'pyproject.toml']

[package]
symbol = "📦 "
style = "bg:#e0af68 fg:#15161e bold"
format = '[ $symbol$version ]($style)'
display_private = false

# --- Docker, Kubernetes, Terraform (Teal #7dcfff) ---
[docker_context]
symbol = " "
style = "bg:#7dcfff fg:#15161e bold"
format = '[ $symbol $context ]($style)'
detect_files = ['docker-compose.yml', 'docker-compose.yaml', 'Dockerfile']
only_with_files = true

[kubernetes]
symbol = "☸ "
style = "bg:#89ddff fg:#15161e bold"
format = '[ $symbol$context( \($namespace\)) ]($style)'
disabled = false
detect_files = ['k8s', 'Dockerfile', 'docker-compose.yml', 'helmfile.yaml']
detect_folders = ['k8s', '.kube', 'kubernetes']

[terraform]
symbol = "💠 "
style = "bg:#89ddff fg:#15161e bold"
format = '[ $symbol$workspace ]($style)'
detect_extensions = ['tf', 'tfplan', 'tfstate']
detect_files = ['.terraform']

# --- AWS/Azure/GCloud ---
# Add after kubernetes/terraform:
[aws]
symbol = " "
style = "bg:#ff9900 fg:#15161e bold"
format = '[ $symbol($profile )(\($region\)) ]($style)'
disabled = true  # Enable only if you use AWS

[gcloud]
symbol = "☁️ "
style = "bg:#4285F4 fg:#15161e bold"
format = '[ $symbol$account(@$domain)(\($region\)) ]($style)'
disabled = true  # Enable only if you use GCP

[azure]
symbol = "󰠅 "
style = "bg:#0078D4 fg:#15161e bold"
format = '[ $symbol($subscription) ]($style)'
disabled = true  # Enable only if you use Azure

# --- System Info (Grey #414868) ---
# Jobs, Cmd, and Battery share this background so they merge seamlessly
[jobs]
symbol = ""
style = "bg:#414868 fg:#c0caf5"
number_threshold = 1
format = '[ $symbol$number ]($style)'

[cmd_duration]
min_time = 2000
style = "bg:#414868 fg:#c0caf5"
format = "[ 󰔟 $duration ]($style)"

[status]
style = "bg:#414868 fg:#f7768e bold"
format = '[$symbol$status]($style)'
symbol = "✘ "
disabled = false
success_symbol = ""
not_executable_symbol = "🚫 "
not_found_symbol = "🔍 "
sigint_symbol = "🧱 "
signal_symbol = "⚡"

# --- THE BATTERY TRICK ---
[battery]
full_symbol = " "
charging_symbol = "󰂄 "
discharging_symbol = " "
format = "[ $symbol$percentage ]($style)"
# Critical (< 20%) - Red
[[battery.display]]
threshold = 20
style = "bg:#414868 fg:#f7768e bold"
# Warning (< 45%) - Orange
[[battery.display]]
threshold = 45
style = "bg:#414868 fg:#ff9e64 bold"
# Healthy (The rest) - Blue/White
[[battery.display]]
threshold = 100
style = "bg:#414868 fg:#c0caf5"

# --- Time (Dark Blue #24283b) ---
[time]
disabled = false
time_format = "%R"
style = "bg:#24283b fg:#7aa2f7 bold"
format = '[  $time ]($style)'

# --- Character ---
[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
vimcmd_symbol = "[❮](bold yellow)"
